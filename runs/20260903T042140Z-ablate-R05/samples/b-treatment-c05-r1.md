I didn't find the cause, but the run history narrows it down considerably, so here's what I have.

The short runs are not randomly scattered. Looking at the last eight runs, the low ones (23902, 22104, 23551 rows) all finished in about seven minutes, while the normal ones (~48k rows) took fifteen minutes or more. So the job isn't failing partway through a full workload; it is doing roughly half the work and completing successfully, which is why every run is marked `ok`. Whatever is going wrong happens before or during `fetch_events`, not in the write loop.

That points at the window calculation in `jobs/scheduler.py:61`: `window_start = last_success_at or (now - timedelta(hours=24))`. The window is anchored to the previous run's success timestamp rather than to a fixed daily boundary, so anything that shifts `last_success_at` shortens the next window. A plausible story is that some other run — a manual invocation, a retry, a backfill — lands between nightly runs and moves `last_success_at` forward, leaving the 02:00 job with only half a day of events to roll up. I could not confirm this, because I only looked at the eight most recent rows and did not check whether the short runs are preceded by an extra `job_runs` entry, nor whether the low runs fall on a consistent weekday, which would suggest a different explanation entirely.

Two other things I ruled out only weakly. The `transaction()` block wraps the whole write loop, so a partial write would roll back rather than commit half the rows, which fits the theory that the shortfall comes from the input rather than the output. And commit `9d02e1e`, "retry transient fetch errors", touches exactly the code path I'm suspicious of; I did not read the diff, and a retry that swallows a partial fetch result would produce these same symptoms without any window bug at all.

The query I'd run next is the full 30 rows of `job_runs` with `window_start` and `window_end` selected alongside the row counts. If the short runs have windows of roughly twelve hours, the scheduler is the cause and the fix is to anchor the window to a fixed boundary. If their windows are a full 24 hours and the counts are still halved, the problem is inside `fetch_events` and that retry commit is where I'd look. Do you know whether anything else — a backfill or an on-call rerun — touches this job outside the 02:00 schedule?