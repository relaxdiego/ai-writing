# Scratch clean-room config dirs — may contain copied OAuth credentials.
.scratch/
*.credentials.json

# Derived: assembled style prompt (its SHA-256 is recorded in each manifest).
build/

# Local env
.envrc.local
.devbox/
