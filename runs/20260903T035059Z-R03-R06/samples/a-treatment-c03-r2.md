Four of the five files that referenced `LegacyConnection` now go through `ConnectionPool`: `src/billing/invoices.py`, `src/billing/refunds.py`, `src/reports/monthly.py` and `src/reports/export.py`. The suite passes after the change, 29 passed and 3 skipped; I did not look into what the three skips cover, so I can't tell you whether any of them would have exercised the code I touched. My grep was also scoped to `src/`, so references in tests, scripts or config are unaccounted for.

The remaining file is `src/admin/backfill.py`, and I left it on the deprecated class deliberately. It opens the connection at `repeatable_read` and iterates `conn.raw_cursor(QUERY, chunk_size=10000)` to stream about 40M rows without materialising them, and a comment in the file records that `ConnectionPool` had no streaming cursor equivalent as of v3.2. A mechanical swap there would have pulled the whole result set into memory, which is a different bug from the one you asked me to fix, so the deprecation is not fully retired yet. Closing it needs one of two things that depend on facts I don't have: a `ConnectionPool` version that exposes a streaming cursor, if one has shipped since v3.2, or a rewrite of the backfill to page through the table by key range with its own bounded queries, which is only equivalent if the `repeatable_read` isolation across the full scan is incidental rather than something the backfill's correctness rests on.