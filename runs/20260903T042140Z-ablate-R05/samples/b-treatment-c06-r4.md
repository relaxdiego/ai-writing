You're right, and I was wrong. Line 34 raises when `status < 500 and status != 429`, so a 400 propagates on the first attempt and never reaches the `sleep`. The only retried cases are 5xx and 429, which is what you'd want. I read the condition as a guard on whether to retry rather than as an immediate re-raise, and that inverted the whole thing; the recommendation to narrow the retry condition doesn't apply, since it's already narrow.