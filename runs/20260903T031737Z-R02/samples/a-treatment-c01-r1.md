`src/cache/session.py` had `session_ttl` hardcoded to 3600 while `src/config/defaults.py` defined `SESSION_TTL_SECONDS = 900`, so the config value was dead — nothing read it. I pointed the class attribute at the constant, which makes the config authoritative but also changes runtime behavior: cached sessions now expire after 15 minutes instead of an hour, four times sooner than before. If 3600 was the intended live value and the config entry was the stale one, the fix should go the other way and update `defaults.py` instead; I assumed the config file was the source of truth, but you're better placed to know which number production actually wants.

`tests/test_session_cache.py::test_expiry_boundary` then failed on `assert 3600 == 900`, and I changed its assertion to compare against `SESSION_TTL_SECONDS` as well. All 8 tests pass now, but that test no longer pins a concrete number — it asserts the constant against itself and will keep passing no matter what value `defaults.py` grows. If the point of that test was to guard the boundary value, it's worth restoring a literal there, either the one you actually want or a separate check that expiry happens on the correct side of the boundary.

One thing I could not confirm: the `get` method at `src/cache/session.py:88` compares against a bare `now`, and I only read lines 80–95, so I never saw where it's bound. The suite passes, so it resolves from somewhere above or from a module-level import, but it's worth a glance if you didn't intend a module-scope timestamp.