`src/cache/session.py` had its own hardcoded `session_ttl = 3600` while `src/config/defaults.py` defined `SESSION_TTL_SECONDS = 900`, so the class attribute now reads from the config constant and the two no longer disagree. `tests/test_session_cache.py::test_expiry_boundary` asserted the literal `3600` and failed once the class changed; I rewrote that assertion to compare against `SESSION_TTL_SECONDS` as well, and the full file passes at 8/8.

Be aware that this is a live behavior change, not just a tidy-up: entries in the session cache now expire after 15 minutes instead of an hour, since the config value was the one that won. The boundary test was the only thing pinning the old duration, and I updated it to follow the constant rather than to defend 3600 — if the hour was the intended production value and the config default is the stale one, the fix belongs in `defaults.py` instead and I should flip it there.

One thing I noticed but did not touch: `get()` at `src/cache/session.py:88` compares against `now` without any assignment to `now` in the method body I read (lines 80–95). It may be injected or imported from outside that range, but if not, expiry would raise on every lookup. Worth a look if you haven't already ruled it out.