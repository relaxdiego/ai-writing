The session cache had its TTL hardcoded as `session_ttl = 3600` on the class, which disagreed with `SESSION_TTL_SECONDS = 900` in `src/config/defaults.py`. I changed the class attribute to reference the config constant, so the cache now expires entries after 15 minutes rather than an hour. That change made `test_expiry_boundary` fail, since the test asserted the literal `3600`; I updated the assertion to compare against `SESSION_TTL_SECONDS` too, and the full `tests/test_session_cache.py` suite now passes at 8 tests.

Two things worth flagging before you take this further. The test failure was arguably telling you something real: the test pinned the old value deliberately, and I resolved that by assuming the config constant is the intended source of truth. If 3600 was the value you actually wanted in production, the fix runs the wrong way and the config default should move instead. Separately, while reading `get()` I noticed `now` is used in the expiry comparison without being assigned anywhere in the method, so that path looks like it would raise a `NameError` when an entry exists. The tests passing suggests nothing in the suite reaches it with a live entry. I left it alone since it is outside what you asked for, but it is worth a look.