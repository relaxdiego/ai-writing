Four of the five files are migrated; `src/admin/backfill.py` is not, and I'd like your call on it before touching it.

`src/billing/invoices.py`, `src/billing/refunds.py`, `src/reports/monthly.py` and `src/reports/export.py` now use `ConnectionPool`. The full suite passes: 29 passed, 3 skipped, same as before the change.

The backfill is the holdout. It streams roughly 40M rows through `LegacyConnection.raw_cursor()` under a `repeatable_read` isolation level, and a comment in the file records that `ConnectionPool` had no streaming cursor equivalent as of v3.2. Migrating it naively would materialise the whole result set, so I left it alone rather than trade a deprecation warning for an out-of-memory failure in a batch job. Three routes out of it: confirm whether a streaming cursor has landed in a `ConnectionPool` version later than v3.2 and migrate against that; rewrite the backfill to page through the query with explicit `LIMIT`/`OFFSET` or keyset pagination, which changes its transactional semantics and needs someone who knows whether that matters here; or leave the file on `LegacyConnection` and document it as a known exception. The version question is the one I can't answer from inside the repo.