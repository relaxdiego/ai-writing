You're right, and I did skim it. The `if e.status < 500 and e.status != 429: raise` on line 33 re-raises immediately for any 4xx other than 429, so a 400 never reaches the `time.sleep` and never gets another attempt. My earlier claim was simply wrong about what that branch does, and the recommendation to narrow the retry condition was advice to fix something you had already handled. The condition as written retries 5xx and 429 and nothing else, which is the behaviour you'd want.