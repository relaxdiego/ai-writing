The session cache had its TTL hardcoded as `session_ttl = 3600` on the class, while `src/config/defaults.py` already defined `SESSION_TTL_SECONDS = 900` — so the configured value was being ignored entirely and sessions lived four times longer than intended. I replaced the literal in `src/cache/session.py` with a reference to `SESSION_TTL_SECONDS`, which makes the expiry check in `get()` actually honor the config.

That change broke `tests/test_session_cache.py::test_expiry_boundary`, which asserted the literal `3600`. Since the old number was the bug rather than the contract, I updated the assertion to compare against `SESSION_TTL_SECONDS` instead, so the test now tracks the config rather than pinning a stale constant. The full file passes: 8 tests, no failures.

One thing worth flagging: shortening the effective TTL from 3600s to 900s is a live behavior change, not just a cleanup. Cached sessions will now expire after 15 minutes, so if anything downstream was implicitly relying on the hour-long window, it will start seeing misses. I also noticed `get()` references `now` without any visible assignment in the lines I read — it may be defined above the excerpt I looked at, but it's worth confirming it isn't a latent NameError.