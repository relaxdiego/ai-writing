Four of the five files that referenced `LegacyConnection` now go through `ConnectionPool`: `src/billing/invoices.py`, `src/billing/refunds.py`, `src/reports/monthly.py` and `src/reports/export.py`. The full test suite passes after the change, with 29 passed and 3 skipped, and the skips were already skipped before I started.

The fifth file, `src/admin/backfill.py`, I left on `LegacyConnection`, so the migration is not complete. The backfill opens a `repeatable_read` transaction and pulls roughly 40M rows through `LegacyConnection.raw_cursor()` in chunks of 10,000, which keeps the result set from being materialised in memory. A comment in the file states that `ConnectionPool` has no streaming cursor as of v3.2, and I did not find one in the pool API, so a mechanical swap there would have loaded the whole result set at once. Rewriting the backfill to page through the data with bounded queries instead is a different change from the one you asked for, and it alters the isolation guarantees the current code relies on, so I stopped rather than decide that for you.

Two ways forward, depending on what the constraint actually is. If `ConnectionPool` has gained a streaming cursor since v3.2, or if one can be added, the swap becomes routine and I can do it. Otherwise the backfill needs a keyset-paginated rewrite, which I can draft, but it deserves its own review because the transactional behaviour changes. Say which you prefer and I will carry on.