The rename is done everywhere except one place, and the suite is red because of it: `HEADERS` in `src/admin/export_csv.py:40` still reads `"mobile"`, and `tests/test_export.py::test_csv_headers` fails with `assert 'phone' in headers`.

Everything else is renamed: 23 occurrences across six files, covering `src/accounts/models.py`, `forms.py`, `serializers.py`, `src/notify/sms.py`, and the two test modules. `tests/test_accounts.py` and `tests/test_notify.py` pass, 20 tests.

I stopped at the CSV header because it is not a field name, it is a column label in a file other people consume. The comment above `row_for` says the finance team imports this file and that changing the names changes what their spreadsheets read, so renaming it is a change to an external contract rather than to our code. The row body was safe to rename and I did: `row_for` now reads `user.phone`, so the exported column carries the same data it always did, just under its old label.

That leaves a genuine conflict I can't settle for you. `tests/test_export.py` asserts the header is `'phone'`; it never contained the string `"mobile"`, so it didn't show up in my initial grep and the failure only surfaced on the full run. Either the export format is meant to follow the database rename, in which case the header changes and finance needs telling, or the format is fixed, in which case the test's expectation is wrong and should assert `'mobile'` against the documented format. My read is the second: a column rename in our schema is not by itself a reason to break a file other teams import, and the comment reads like someone has been bitten by this before. I have left both the header and the failing test untouched so the decision stays visible rather than buried under a green suite.