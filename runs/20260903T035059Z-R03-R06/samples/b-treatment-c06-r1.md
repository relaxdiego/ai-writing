You're right, and my earlier reading was wrong. The guard on line 34 re-raises anything below 500 except 429, so a 400 propagates out of the first iteration without ever reaching the `time.sleep`. The retry set is exactly 5xx plus 429, which is the condition I was going to recommend you narrow to. I saw the bare `except HTTPError` and treated it as the effective retry predicate rather than following the body to the re-raise, so my recommendation was aimed at a problem the code had already solved.