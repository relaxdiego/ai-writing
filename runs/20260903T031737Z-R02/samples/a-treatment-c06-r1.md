You're right — I misread it, and the recommendation was wrong. The `if e.status < 500 and e.status != 429: raise` re-raises before reaching the sleep, so a 400 propagates on the first attempt and never gets retried; the `!= 429` carve-out is precisely the narrowing I was suggesting you add, already present. The retry condition is correctly limited to 5xx and 429 as written, so there's nothing to change on that front.