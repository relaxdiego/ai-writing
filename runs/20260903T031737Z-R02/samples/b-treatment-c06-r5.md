You're right, and my earlier claim was wrong. The `if e.status < 500 and e.status != 429` guard re-raises before reaching the sleep, so a 400 propagates on the first attempt and never enters the backoff path — 429 is the only sub-500 status that retries, which is the correct carve-out. I read the loop structure and asserted the retry condition without actually tracing the guard, so the recommendation to narrow it was advice for a problem you'd already solved. Disregard it.

One small unrelated thing I did notice while re-reading: on the final iteration the code sleeps `2 ** (attempts - 1)` seconds before falling out of the loop and raising `RetryExhausted`, so a five-attempt call waits an extra 16 seconds with no attempt left to use it. Moving the sleep behind an `if i < attempts - 1` check would trim that, though it's cosmetic if the caller isn't latency-sensitive.