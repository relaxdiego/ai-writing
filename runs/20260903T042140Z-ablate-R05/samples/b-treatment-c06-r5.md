You're right, and I was wrong. The condition on line 33 re-raises whenever the status is below 500 and isn't 429, so a 400 propagates out of the loop on the first attempt and never reaches the `time.sleep`. Only 5xx responses and 429 fall through to the backoff, which is the behaviour you'd want. My recommendation to narrow the retry condition was based on a misreading, so there's nothing to change here; disregard it.