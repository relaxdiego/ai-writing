I did not find the cause. What I have is a clear shape in the run history and two candidates that fit it.

The last eight runs split cleanly in two. Five wrote about 48,000 rows and took roughly fifteen minutes; three wrote about 23,000 and took roughly seven. All eight recorded status `ok`, so nothing is failing loudly. The important detail is that duration scales with row count instead of staying flat: a run that fetched a full day of events and then lost half of them on the way out would still spend the full time fetching and grouping. So the short nights are almost certainly seeing less input, not dropping writes. That also fits the structure of `run()` in jobs/nightly_rollup.py:20, where every `write_rollup` happens inside one `transaction()`, so a failure partway through should yield zero rows rather than half.

The short runs fall on Aug 23, 26 and 29, exactly three days apart. I can't account for that period from what I read; the scheduler has no three-day component in it. The pattern predicts Aug 20 and Sep 1 as short as well, and there are 30 rows in `job_runs`, so it can be confirmed or killed without waiting for another night.

The first candidate is the window. jobs/scheduler.py:61 sets `window_start = last_success_at or (now - timedelta(hours=24))`. If anything updates `last_success_at` between nightly runs, or if it lands around twelve hours before the 02:00 start, the job legitimately fetches half a day of events and everything downstream behaves exactly as observed. I did not check what `last_success_at` held on those three nights, or whether `job_runs` records the window at all; if it doesn't, that column is worth adding before anything else. The second candidate is 9d02e1e, "fix: retry transient fetch errors". If that wrapper returns whatever pages it accumulated when retries are exhausted instead of raising, `fetch_events` comes back short and the run still finishes and records `ok`. I have not read that diff.

The check that separates them is the window: capture `window_start` and `window_end` per run and compare the two groups. A ~12h window on the short nights puts the fault in the scheduler's success bookkeeping; a full 24h window that still yields half the events puts it in `fetch_events` and its retry path.