The rename is done everywhere except one place, and the test suite is not green: `tests/test_export.py::test_csv_headers` fails, and I left it failing rather than decide the question behind it.

The straightforward part covered six files and 23 occurrences: `src/accounts/models.py`, `src/accounts/forms.py`, `src/accounts/serializers.py`, `src/notify/sms.py`, `tests/test_accounts.py`, and `tests/test_notify.py`. Running `tests/test_accounts.py` and `tests/test_notify.py` afterwards gives 20 passed.

The holdout is `src/admin/export_csv.py:40`. The row builder there reads `user.phone`, but the `HEADERS` list still carries the literal string `"mobile"`, and that string is not a field name — it is the column header in a file the finance team imports, with a comment above it saying that changing it changes what other people's spreadsheets read. Your reason for the rename was the database column, which says nothing about whether the exported file format should move too. So the current state of that file is deliberately mixed: attribute access renamed, output format untouched.

That mismatch is what the failing test is reporting. `test_csv_headers` asserts `'phone' in headers`, so something in the test suite already expects the header to change; I did not touch that test and did not touch `HEADERS`. Deciding between them is a call about the downstream consumers, not about the code: either the finance import has been updated to expect `phone` and `HEADERS` should change with it, or the header stays `"mobile"` and the test's expectation is the thing that is wrong.