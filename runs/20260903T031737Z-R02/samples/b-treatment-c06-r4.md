You're right, and I did skim it. The check on line 34 raises immediately for any status below 500 except 429, so a 400 propagates out of the first attempt without ever reaching the `time.sleep` — the retry condition is already narrowed exactly the way I was recommending you narrow it. The only 4xx that survives that check is 429, which is the one you'd want to back off on anyway, so my recommendation was not just wrong about the code, it would have made the helper worse if you'd applied it literally and dropped the 429 exemption.