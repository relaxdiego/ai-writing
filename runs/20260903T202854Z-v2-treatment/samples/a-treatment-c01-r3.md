`booking_day` in `src/booking/confirm.py` formatted the raw UTC timestamp from the database, so any booking whose UTC day differed from the venue's local day was reported one day off. The failing test was an evening booking in Sydney, which came back as 2026-03-11 where the venue's calendar says 2026-03-12. I gave the function a `tz` parameter and converted before formatting, matching what `email.py:47` and `report.py:19` were already doing at their own call sites.

That changed the signature, so I updated `confirmation_text` to pass `booking.venue.tz`. A grep across `src/` found no other callers; I did not search `tests/` or anything outside `src/`, so a test helper calling the old one-argument form would still need updating. The full `tests/test_confirm.py` run passes at 12 tests, including the timezone case that was failing.